# archiso hooks for live ISO boot
# zfs hook added for ZFS support
# PXE hooks removed (archiso_pxe_*) - not needed for USB/local boot
HOOKS=(base udev microcode modconf kms memdisk archiso archiso_loop_mnt zfs block filesystems keyboard)
COMPRESSION="zstd"
COMPRESSION_OPTIONS=(-T0)
